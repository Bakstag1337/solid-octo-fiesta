#!/bin/bash
set -e

# Update desktop database
if [ -x /usr/bin/update-desktop-database ]; then
    update-desktop-database -q
fi

echo "✅ System Monitor installed successfully!"
echo "Run 'sysmon' to start or find it in your application menu."

exit 0
